# notebooks

EDA and experiment notebooks go here. Empty as of 2026-09-17 (session 1) —
no data downloaded yet. First notebook should load `data/raw/*.parquet` and
validate the column/schema assumptions documented in `docs/02-dataset.md`.
