# tests

pytest suite. Empty as of 2026-09-17 (session 1) — add tests alongside each
`src/casmi/` submodule as it's implemented (esp. the InChIKey14/MRR@25 local
scorer in `eval/`, since correctness there underpins all offline validation).
